apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
  - host: 192.168.6.25
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: Ingress-service
            port:
              number: 8083



          
      
